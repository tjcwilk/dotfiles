echo "========== Setting up symlinks =========="

DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

set -e

# -----------------------------------------------------------------------------
# Symlinking inputrc file
# -----------------------------------------------------------------------------
echo "--> .inputrc"
ln -sf $DIR/rcfiles/inputrc ~/.inputrc

# -----------------------------------------------------------------------------
# Finished
# -----------------------------------------------------------------------------
echo "✅ Symlink complete!"
